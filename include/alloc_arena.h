#ifndef ALLOC_ARENA_H
#define ALLOC_ARENA_H

// A simple arena allocator
void arena_allocator(long startaddr, long length);

// No more public methods, its not required to call the underlying methods

#endif
