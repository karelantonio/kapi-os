#ifndef MATH_H
#define MATH_H

// General utilities for Math

// Minimum of two numbers
#define min(a,b) ( ( (a)<(b) )?(a):(b) )

// Maximum of two numbers, take care when both number have different size/signed-ness
#define max(a,b) ( ( (a)>(b) )?(a):(b) )

#endif
